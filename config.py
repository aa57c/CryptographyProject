# Database config file, will leave it in case we need it. Docker should take care of this now.
# import os

# class Config:
#     SQLALCHEMY_DATABASE_URI = os.environ.get('DATABASE_URL')
#     SQLALCHEMY_TRACK_MODIFICATIONS = False
